// A file to play with the code
import java.util.regex.Pattern;
import java.util.*;

import java.io.*;

public class Scratch{

        public static void main (String[] args){
                String titsOut = "breadjambread";
                String titsIn = "hello pentile flavor";
                char ch;

                
                 Scanner s = new Scanner(System.in);
                 String nextLine;
                 
                 System.out.println("enter the string");
                 titsOut = s.next();
                 titsOut = titsOut.substring(0,5);
                 System.out.println(titsOut);
                // while(s.hasNextLine()){
                //         nextLine = s.nextLine();
                //         for(int i=0;i<nextLine.length();i++){
                //         ch = nextLine.charAt(i);
                //         titsIn = nextLine(i)
                //                 ;System.out.println(titsIn);
                //         titsOut = titsOut + ch;}
                // }
                // System.out.println(titsOut);
                // System.out.println(args[0]);
                // int argsLength = args.length;
                // System.out.println(argsLength);
                //  ;
                // String titsOut = "";
                // int position;
                // position = titsIn.indexOf("{");
                // titsOut = titsOut + titsIn.substring(0,position+1) + position;
                // //System.out.println(position);
                // titsIn = titsIn.substring(position+1);
                
                // //System.out.println(titsIn);
                // //System.out.println(titsOut);
                // position = titsIn.indexOf("{");
                // //System.out.println(position);
          }
        
}
        
